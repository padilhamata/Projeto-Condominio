@model ProjetoCondominio.Models.Pessoa

@{
    //https://github.com/acenelio/workshop-asp-net-core-mvc/blob/master/SalesWebMvc/Views/Departments/Create.cshtml
    ViewBag.Title = "Home Page";
}

<form >
        <h1>Sistema de condominios</h1>

        <div class="form-group">
            <label for="exampleInputEmail1">Email address</label>
            @Html.EditorFor(model => model.Login, new { htmlAttributes = new { @class = "form-control" } })
            <small id="emailHelp" class="form-text text-muted">We'll never share your email with anyone else.</small>
        </div>
        <div class="form-group">
            <label for="exampleInputPassword1">Password</label>
            @Html.EditorFor(model => model.Senha, new { htmlAttributes = new { @class = "form-control" } })
        </div>
        <input type="submit" value="Enviar" class="btn btn-default" />
    </form>


    <script>
        @*$(#enviar).on("Click", enviar);

        function enviar{
            $.ajax({
                type: "GET",
                url: "@Url.Action("testaDados", "Home")",
                datatype: "json",
                method: 'GET',
                async: false,
                data: { Login: false },
                sucess: function (response) {
                    console.log(response);
                }
            })
        }*@




     @*$("#todosFuncionarios").on("click", mostrarFuncionarios);

            function mostrarFuncionarios() {

                if ($("#todosFuncionarios:checked").val()) {
                    $("#todosFuncionarios").val(true);

                    $.ajax({
                        type: "GET",
                        url: "@Url.Action("listarTodos", "Diario")",
                        datatype: "json",
                        method: 'GET',
                        async: false,
                        data: { todosFuncionarios: true },
                        success: function (response) {
                            console.log(response);
                            console.log(response.retorno.Data[0].NOME);
                            var array = response.retorno.Data;
                            console.log(array);
                            $('.sumo3').empty();

                            array.forEach(function (item, indice, array) {
                                console.log(item, indice, "item");
                                $('.sumo3').append("<option value='' selected='selected'>" + item.NOME + "</option>");
                            })
                        }
                    });




                }
                else {
                      $("#todosFuncionarios").val(false);

                 $.ajax({
                    type: "GET",
                    url: "@Url.Action("listarTodos", "Diario")",
                    datatype: "json",
                    method: 'GET',
                     async: false,
                     data: { todosFuncionarios: false },
                     success: function (response) {
                         var array = null;
                         console.log(response);
                         console.log(response.retorno.Data[0].NOME);
                          array = response.retorno.Data;
                         $('.sumo3').empty();
                         console.log(array);
                         array.forEach(function (item, indice, array) {
                             console.log(item, indice, "item");
                             $('.sumo3').append("<option value='' selected='selected'>" + item.NOME + " </option>");
                         })
                     }
                });




            }

           ajaxProcessamento = atualizaFormulario("#formDiario", "#listagem_Diario");
         }




                 $.ajax({
                    type: "GET",
                    url: "@Url.Action("listarTodos", "Diario")",
                    datatype: "json",
                    method: 'GET',
                     async: false,
                     data: { todosFuncionarios: false },
                     success: function (response) {
                         var array = null;
                         console.log(response);
                         console.log(response.retorno.Data[0].NOME);
                          array = response.retorno.Data;
                         $('.sumo3').empty();
                         console.log(array);
                         array.forEach(function (item, indice, array) {
                             console.log(item, indice, "item");
                             $('.sumo3').append("<option value='' selected='selected'>" + item.NOME + " </option>");
                         })
                     }
                });




         *@

    </script>
